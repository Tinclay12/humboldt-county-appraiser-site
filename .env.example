# Site URL (for sitemap, canonical URLs, etc.)
NEXT_PUBLIC_SITE_URL=https://humboldtcountyappraiser.com

# Cloudflare Turnstile (free CAPTCHA)
# Get keys from https://dash.cloudflare.com/ -> Turnstile
# For local development, use Cloudflare test keys (always pass):
#   Site key: 1x00000000000000000000AA
#   Secret key: 1x0000000000000000000000000000000AA
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_site_key
TURNSTILE_SECRET_KEY=your_secret_key

# Email Configuration
# Options: none (logs only), smtp, resend
EMAIL_PROVIDER=smtp

# Where contact form submissions are sent
CONTACT_EMAIL=appraisal@tidwellassociates.com

# SMTP (for EMAIL_PROVIDER=smtp)
# Gmail: smtp.gmail.com:587, enable 2FA and create App Password
# Brevo: smtp-relay.brevo.com:587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-business@gmail.com
SMTP_PASS=your_app_password

# Optional: Custom "from" address (defaults to SMTP_USER if not set)
# SMTP_FROM=noreply@humboldtcountyappraiser.com

# Resend (only if EMAIL_PROVIDER=resend)
# RESEND_API_KEY=re_xxxxxxxxxxxx
